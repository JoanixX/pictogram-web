# Configure aquí la URL base para el endpoint de chat.
# Por ejemplo, si tu backend FastAPI expone /api/v1/chat en el mismo host:
VITE_API_URL=/api/v1/chat

# Si tu backend está en otro host en desarrollo (ejemplo):
# VITE_API_URL=http://localhost:8000/api/v1/chat
